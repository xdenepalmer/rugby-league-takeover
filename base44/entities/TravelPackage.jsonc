{
  "name": "TravelPackage",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Package name"
    },
    "description": {
      "type": "string",
      "description": "Package description"
    },
    "image_url": {
      "type": "string",
      "description": "Package image URL"
    },
    "is_coming_soon": {
      "type": "boolean",
      "default": true,
      "description": "Whether package is coming soon"
    },
    "booking_url": {
      "type": "string",
      "description": "Booking / promo link — renders as a click-through button on the package card"
    },
    "booking_label": {
      "type": "string",
      "default": "Book Now",
      "description": "Label for the booking button (e.g. \"Book Circa Rate\")"
    },
    "sort_order": {
      "type": "number",
      "default": 1,
      "description": "Display order"
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": true,
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}