{
  "name": "Testimonial",
  "type": "object",
  "properties": {
    "author_name": {
      "type": "string",
      "description": "Name of the person giving the testimonial"
    },
    "author_role": {
      "type": "string",
      "description": "Optional descriptor, e.g. 'Eels fan · Sydney' or 'Vegas 2025 traveller'"
    },
    "quote": {
      "type": "string",
      "description": "The testimonial text"
    },
    "avatar_url": {
      "type": "string",
      "description": "Optional photo/avatar of the person"
    },
    "rating": {
      "type": "number",
      "description": "Optional star rating 1-5"
    },
    "sort_order": {
      "type": "number",
      "default": 1
    },
    "is_published": {
      "type": "boolean",
      "default": true,
      "description": "Visitor submissions arrive false (pending) until an admin approves"
    },
    "ip_address": {
      "type": "string",
      "description": "Submitter IP (admin-only PII, for moderation/ban context)"
    },
    "user_email": {
      "type": "string",
      "description": "Email of the submitter when logged in"
    },
    "user_id": {
      "type": "string",
      "description": "User id of the submitter when logged in"
    }
  },
  "required": [
    "author_name",
    "quote"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.is_published": true
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
